global:
  scrape_interval: 15s

scrape_configs:
  - job_name: localhost
    static_configs:
      - targets:
          - 'localhost:3001'
  - job_name: node-exporter
    static_configs:
      - targets:
          - 'node-exporter:9100'
